import java.io.*;
import java.util.Random;

public class ArabicWordleMain {
    /**
     * Main method of the program, reads through the answerList file and creates the GUI
     * from the answer line generated
     * @param args input of the main method
     */
    public static void main(String[] args){
        ArabicWordleGUI gui = new ArabicWordleGUI();
    }
}
